
pre-commit:
  parallel: true
  commands:
    # format:
    #   run: yarn format
    #   stage_fixed: true
    #   glob: "**/*.{ts,tsx}"
    lint-next:
      run: yarn lint:next
      stage_fixed: true
      glob: "**/*.{ts,tsx}"
    # lint:
    #   run: yarn lint
    #   stage_fixed: true
    #   glob: "**/*.{ts,tsx}"